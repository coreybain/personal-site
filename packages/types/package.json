{
  "name": "@home/types",
  "version": "0.0.0",
  "private": true,
  "description": "Zod schemas for the Convex data model — the single API contract shared by apps/web, packages/convex, tooling/collector and (via codegen) apps/ios.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "sideEffects": false,
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
